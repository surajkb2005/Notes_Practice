services:
  backend:
    build:
      context: ./backend
    image: notes-backend
    container_name: notes-backend-container
    ports:
      - "3000:5000"
    environment:
      - PORT=5000
      - MONGODB_URI=mongodb://host.docker.internal:27017/notesdb
    restart:
      unless-stopped